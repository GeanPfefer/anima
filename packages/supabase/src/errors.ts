import {failure,type WorkOperationResult} from '@anima/core';
export interface SupabaseFailure{code?:string;message?:string;details?:string;hint?:string}
export const mapSupabaseFailure=<T>(e:SupabaseFailure,mutation:boolean):WorkOperationResult<T>=>{const m=(e.message??'').toLowerCase();if(e.code==='P0002')return failure('work_item_not_found','Item de trabalho não encontrado.');if(e.code==='40001')return failure('version_conflict','O item mudou desde a última leitura.');if(e.code==='22023')return failure(m.includes('transition')?'invalid_transition':'invalid_input','A operação não é válida no estado atual.');if(e.code==='42501'){if(m.includes('authentication'))return failure('authentication_required','Autenticação necessária.');if(m.includes('allowlist')||m.includes('enabled'))return failure('orchestration_not_enabled','Orquestração não habilitada.');if(m.includes('source')||m.includes('message'))return failure('source_message_not_eligible','Mensagem de origem não elegível.');return failure('permission_denied','Operação não permitida.');}if(mutation&&!e.code)return failure('ambiguous_outcome','Não foi possível confirmar o resultado. Reconcilie antes de repetir.',false,e);return failure('persistence_failure','Não foi possível acessar a orquestração.',true,e);};
